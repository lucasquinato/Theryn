/**
 * File: Player.ts
 * Path: src/game/components/
 */

/**
 * Marker component identifying an entity as the player-controlled character.
 *
 * The component intentionally contains no state. Systems may query it together
 * with other components to locate the current player entity without depending
 * on a specific entity identifier.
 */
export class Player {}
